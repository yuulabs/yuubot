# Copy to .env and fill in the keys you use.
#
# yagents up will auto-load .env (searching upward from the current directory).
# You can also pass an explicit path: yagents up --dot-env /path/to/.env

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
TAVILY_API_KEY=