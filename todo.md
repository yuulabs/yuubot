prompt重构：

统一yuuagents为flow机制。提供ping工具（允许父子agent互相ping，允许agent和交互式命令行交互）。

完事之后准备部署问题：

1. docker 内身份验证问题。一个很麻烦的问题（比如说bot需要登陆网站怎么办）。也许不能100%解决。
2. 扩展消息渠道，避免依赖于qq（如果qq被封了就没了）
3. 一键部署脚本（中日英，包括disclaimer. 尽量避免使用qq的法律风险）。目标是一行命令，出现一个交互式脚本。有deepseek api-key + tavily api key（opt但是最好有）即可完成部署。需要处理多个仓库的依赖问题（改成依赖云端版本）。
4. 热配置。增强现在的配置文件做法，允许bot实时修改自己的配置（如模型，人格）

## TODO

yuuagents需要提供扩展自定义工具的接口。im需要变成一个工具而不是一个skill（它似乎总是被需要，同时用工具就可以简单地避免JSON在CLI里的解析问题了）。

提供解析合并转发消息（json消息）的能力。这样可以cover 80%的长文字请求。

供应商动态配置模型（同上面的热加载）。允许从供应商处获取模型，在qq配置；允许从供应商处获得模型能力（例如vision）。

添加专属的yuubot请求头

傻瓜一键交互式安装包（Win(在win上运行的脚本但实际用WSL2配) + Linux）

宣传网站。敲定人设图片和视觉，动画效果（拒绝纯文档硬摆）

yuutrace渲染专业化，增加过滤器等。让它初步可用，后面可以等开源来贡献。